---
name: writing-pm-prd
description: 撰写高质量的结构化产品需求文档（PRD）。当你需要写 PRD、产品需求文档、需求规格说明书、功能需求文档，或者需要整理产品需求、准备技术评审需求文档时使用。支持输出为飞书在线文档或本地 Markdown，并自动创建飞书文档、关联 OKR 和知识库归档。适用于移动端 App、小程序、Web、SaaS、平台工具等产品。
---

# 撰写高质量结构化产品需求文档（PRD）

帮助用户按行业最佳实践撰写结构化 PRD，融合腾讯产品经理 PRD 框架，支持飞书文档和本地 Markdown 双场景输出。

## 工作流程

### 第一步：需求澄清

不要直接动笔。先了解最小可启动信息：

**核心信息（尽量获取）**：

1. **问题**：这个需求解决什么问题？用户当前遇到了什么困难？
2. **用户**：目标用户是谁？有什么特征？
3. **成功标准**：怎么判断这个需求做成了？
4. **时间约束**：有明确的 deadline 或版本节点吗？
5. **参考资料**：有原型图、竞品参考、设计稿吗？

**最小可启动原则**：

- 以上 5 点不必全部满足才动笔。如果用户对部分问题暂时没有答案，可以带着合理假设先写。
- 如果信息不足但用户希望先出稿，生成 **「带假设的 V0.1 草稿」**，并在文档顶部用醒目的 **「待确认问题」** 区块列出所有假设项和待补充信息，方便用户快速 review。
- 用户可以拿着 V0.1 草稿去对齐相关方，再回来迭代。

### 第二步：输出格式与篇幅确认

如果用户没有明确输出格式，询问：

> 你希望输出为 **飞书在线文档**（自动创建、关联 OKR、归档知识库）还是 **本地 Markdown 文件**？

同时判断 PRD 篇幅等级（见下方「篇幅分级」），如果用户没有明确说明，根据需求复杂度自动判定并告知用户：

- **Lite**：小改动 / 运营配置 / 后台字段调整
- **Standard**：常规功能需求
- **Full**：复杂项目 / 跨团队协作 / 需技术评审

### 第三步：PRD 撰写

按照下方模版结构生成 PRD，根据篇幅等级决定每章的展开程度（详见「篇幅分级」）。

### 第四步：飞书集成（仅飞书文档模式）

如果用户选择飞书文档输出：

1. 使用 `lark-doc` skill 创建飞书文档，使用当前已认证的飞书用户身份；如需指定归属人，先询问用户
2. 询问用户是否需要关联 OKR（如果用户没有主动提供 OKR 信息）
3. 如果需要关联 OKR，使用 `lark-okr` skill 创建
4. 询问用户是否需要归档到知识库（如果用户没有指定知识库位置）
5. 如果需要归档，使用 `lark-wiki` skill 操作

### 第五步：评审准备

PRD 生成后，可选提供：
- 评审要点清单（关键决策项、风险点、争议预判）
- 面向不同角色的阅读指引（设计师看什么、前端看什么、后端看什么、测试看什么）

### 第六步：输出前自检

PRD 完成初稿后，对照以下自检清单逐项确认：

| # | 检查项 | 说明 |
|---|--------|------|
| 1 | **问题定义明确** | 读者能否一句话说清「这个需求要解决什么问题」 |
| 2 | **目标用户清晰** | 是否明确了「谁会使用、在什么场景下使用」 |
| 3 | **成功指标可衡量** | 是否有量化指标（而非「提升体验」等模糊表述） |
| 4 | **范围边界明确** | 是否列出了 In Scope 和 Out of Scope |
| 5 | **优先级标注** | 每个功能点是否标注了 P0/P1/P2 |
| 6 | **异常场景覆盖** | 是否覆盖了网络异常、数据为空、权限不足、操作失败等 |
| 7 | **埋点需求完整** | 关键用户行为和业务结果是否都有对应埋点 |
| 8 | **依赖与风险** | 是否列出了外部依赖、技术风险及缓解措施 |
| 9 | **待确认问题** | 所有假设和待确认项是否已在文档顶部醒目列出 |
| 10 | **可测试性** | 每个功能点是否有明确的验收标准（Given/When/Then） |

如有未通过项，在文档顶部「待确认问题」区块中标注，或回到对应章节补充。

---

## 篇幅分级

根据需求复杂度，PRD 分为三档。不适用章节写 **N/A + 一句话原因**，不要强制展开；但核心章节（背景、问题、范围、功能描述）任何档位都不能跳过。

### Lite（轻量级）
适用场景：小改动、运营配置、后台字段调整、Bug 修复类需求。

| 章节 | 处理方式 |
|------|----------|
| 文档信息 | 精简为 3-4 行 |
| 需求背景 | 2-3 句话 |
| 需求问题 | 1-2 句话 |
| 时机分析 | N/A（除非有明确时间约束） |
| 范围与边界 | 列表即可 |
| 业务流程 | 仅关键步骤，不需要 Mermaid |
| 功能清单 | 1-2 行 |
| 需求描述 | 仅写前置条件 + 验收标准，不展开交互说明 |
| UI 页面 | N/A（除非有页面变更） |
| 数据埋点 | N/A（除非涉及新埋点） |
| 非功能需求 | N/A |
| 风险与依赖 | 仅写有关键依赖时 |
| 附录 | N/A |

### Standard（标准级）
适用场景：常规功能需求，涉及前后端协作、有 UI 变更。

所有章节正常展开，按模版完整撰写。每个功能点包含验收标准。

### Full（完整级）
适用场景：复杂项目、跨团队协作、需要技术评审、涉及核心流程变更。

在 Standard 基础上额外要求：
- 业务流程图（Mermaid）
- 完整异常流程表
- 详细的交互说明和状态处理
- 性能/安全/兼容性非功能需求必须逐项填写
- 必须有关联 OKR 和知识库归档

---

## PRD 模版结构

以下为标准模版结构（Standard 级别）。Lite 和 Full 级别按上方篇幅分级调整。

### 一、文档信息

```markdown
| 项目 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 产品名 | [产品名称] |
| 子系统/模块 | [子系统名称] |
| 编写人 | [姓名] |
| 编写日期 | YYYY-MM-DD |
| 项目成员 | [角色]：[姓名]；[角色]：[姓名] |

**修订记录**

| 版本 | 修订人 | 修订日期 | 修订描述 |
|------|--------|----------|----------|
| V1.0 | [姓名] | YYYY-MM-DD | 初稿 |
```

> **待确认问题**（V0.1 草稿时必填，正式版可移除）：
> 
> - [ ] [假设项 1] —— 假设依据：[原因]，待与 [谁] 确认
> - [ ] [假设项 2] —— 假设依据：[原因]，待与 [谁] 确认
> - [ ] [信息缺口 1] —— 需要 [谁] 提供

### 二、需求背景

**背景概述**：项目的业务背景、市场环境、触发因素。用 2-3 句讲清楚「为什么会有这个需求」。

**需求来源**：谁（角色/部门）提出的、通过什么渠道（用户反馈/数据分析/战略规划/竞品对标）。

**目标用户角色**：

| 用户角色 | 角色描述 | 核心诉求 |
|----------|----------|----------|
| [角色名] | [一句话描述] | [TA 最想解决什么问题] |

**产品目标**：本次需求期望达成的业务目标（定性或定量）。

### 三、需求问题是什么

**核心问题陈述**：用 1-2 句话说清楚现状存在的问题。格式：「当前……导致……」。

**用户痛点**：

1. [痛点 1]：[具体场景下的困难]
2. [痛点 2]：[具体场景下的困难]

**解决思路概述**：大方向上如何解决，不必展开细节。

### 四、为什么是现在处理

**时机分析**：为什么不能延迟处理？（如：配合运营活动节点、竞品已上线类似功能、用户反馈集中爆发、技术债务累积到影响可用性）

**与其他机会的权衡**：当前需求优先于其他候选需求的原因。

**不处理的后果**：如果现在不做，会导致什么。

### 五、需求范围与边界

**范围内（In Scope）**：

- [功能 A]
- [功能 B]

**范围外（Out of Scope）**：

- [不做的功能 X] —— 原因：[为什么本次不做]
- [不做的功能 Y] —— 原因：[为什么本次不做]

**假设与约束条件**：

- [假设 1]
- [约束 1]（如：仅支持 iOS 14+、依赖第三方接口）

### 六、业务流程 / 功能流程图

**核心流程**：用文字描述 + ASCII 流程图或 Mermaid 图表描述主流程，覆盖正常路径。

推荐使用 Mermaid 语法以支持可视化渲染：

```mermaid
graph TD
    A[用户进入页面] --> B{条件判断}
    B -->|满足条件| C[执行操作]
    B -->|不满足条件| D[提示引导]
    C --> E[获得结果]
```

**异常流程**：列出关键异常分支及其处理逻辑。

| 异常场景 | 触发条件 | 处理逻辑 |
|----------|----------|----------|
| [场景名] | [条件] | [如何处理] |

### 七、功能清单

| 功能模块 | 功能点 | 细分功能点 | 功能描述 | 优先级 |
|----------|--------|--------|----------|--------|
| [模块A] | [功能点1] | [细分1] | [一句话说明] | P0 |
| [模块A] | [功能点1] | [细分2] | [一句话说明] | P0 |
| [模块A] | [功能点2] | [细分1] | [一句话说明] | P1 |

**优先级定义**：

- **P0**：必须有（MVP，缺少则产品不可用）
- **P1**：应该有（核心体验，缺少则体验不完整）
- **P2**：可以有（锦上添花，资源允许再做）

### 八、需求描述（核心部分）

按功能点逐个拆分，**每个功能点必须遵循以下结构**：

---

**功能点：[功能点名称]**

> **一句话总结**：[用一句话说清楚这个功能点要做什么，加粗突出]

**用户场景**：什么用户在什么情况下会用到此功能。

**前置条件**：触发该功能需要满足的条件（如：已登录、已完成实名认证、网络正常）。

**流程说明**：

| 步骤 | 用户操作 | 系统响应 |
|------|----------|----------|
| 1 | [用户做什么] | [系统怎么响应] |
| 2 | [用户做什么] | [系统怎么响应] |

**交互说明**：

- 页面元素：[列举关键页面元素]
- 手势操作：[如支持的手势：点击/滑动/长按/双击]
- 转场动画：[页面切换方式]
- 加载状态：[加载中展示什么]

**异常情况处理**：

| 异常场景 | 触发条件 | 系统行为 |
|----------|----------|----------|
| 未登录 | 用户未登录时触发功能 | 引导登录，保留上下文 |
| 网络异常 | 请求超时或无网络 | 提示「网络异常，请稍后重试」并提供重试按钮 |
| 权限不足 | 用户无权限访问功能 | 提示说明原因并引导申请权限 |
| 数据为空 | 列表/搜索结果为空 | 展示空状态插图和引导文案 |
| 操作失败 | 提交/保存失败 | 提示错误原因并提供重试 |

**验收标准**（Given / When / Then）：

> 用 Given / When / Then 格式编写，确保研发和测试可以直接转化为测试用例。

| # | Given（前置条件） | When（用户操作） | Then（预期结果） |
|---|-------------------|------------------|------------------|
| 1 | [前置状态] | [触发动作] | [系统应呈现什么/发生什么] |
| 2 | [前置状态] | [触发动作] | [系统应呈现什么/发生什么] |

**测试要点**：

- 功能测试：[需要重点验证的功能路径]
- 兼容性测试：[需要关注的设备/系统/浏览器版本]
- 回归测试：[可能受影响的其他功能模块]

**边界条件**：

| 边界场景 | 边界值 / 条件 | 预期行为 |
|----------|--------------|----------|
| [如：输入上限] | [如：超过 500 字] | [如：截断并提示] |
| [如：并发操作] | [如：两人同时编辑] | [如：后提交者覆盖，并提示冲突] |
| [如：极限数据量] | [如：列表 10000 条] | [如：分页加载，每页 20 条] |

**补充说明**：[其他需要研发注意的细节、设计参考链接等]

---

### 九、原型页面 / UI 页面

**页面清单**：

| 页面名称 | 页面路径 | 页面说明 |
|----------|----------|----------|
| [页面A] | [/path] | [一句话说明] |

**页面间跳转关系**：描述页面之间的流转逻辑，可用 Mermaid 或文字描述。

**关键页面原型说明**：对核心页面的布局结构、关键元素、交互逻辑进行文字描述。如果用户提供了原型图或设计稿链接（Figma、蓝湖等），在此引用。

### 十、数据埋点与衡量指标

**成功指标**：

| 指标 | 当前值 | 目标值 | 说明 |
|------|--------|--------|------|
| [指标名] | [基线] | [目标] | [为什么选此指标] |

**埋点需求**：

| 事件ID | 事件名称 | 触发条件 | 自定义参数 |
|--------|----------|----------|------------|
| [event_id] | [名称] | [何时触发] | [参数名: 含义] |

### 十一、非功能需求

**性能需求**：响应时间（如页面加载 <2s）、并发量、可用性（如 99.9%）。

**兼容性需求**：

| 维度 | 要求 |
|------|------|
| 操作系统 | [iOS 15+, Android 11+] |
| 浏览器 | [Chrome 90+, Safari 15+] |
| 屏幕尺寸 | [最小 320px, 最大 1440px] |

**安全需求**：数据加密、权限校验、隐私合规（如 GDPR/个保法）、敏感信息脱敏。

### 十二、风险与依赖

| 风险类型 | 风险描述 | 概率 | 影响 | 缓解措施 |
|----------|----------|------|------|----------|
| 技术风险 | [描述] | 高/中/低 | 高/中/低 | [措施] |
| 业务风险 | [描述] | 高/中/低 | 高/中/低 | [措施] |
| 资源风险 | [描述] | 高/中/低 | 高/中/低 | [措施] |

**外部依赖**：

- [依赖的系统/团队/接口] —— 影响：[什么影响]，对接人：[姓名]

### 十三、附录

- **相关文档**：[链接]
- **评审会议记录**：[链接或此处记录]
- **术语表**：

| 术语 | 说明 |
|------|------|
| [术语] | [解释] |

---

## 撰写原则

以下原则贯穿整个 PRD 撰写过程，优先级高于模版细节：

### 结论先行
每个章节先给总结再分点。让读者快速判断「这部分跟我有没有关系」。

### 从宏观到微观
先写总体（流程、清单、架构），再写具体需求细节。读者应该能在 3 分钟内判断这个需求的整体面貌。

### 异常情况必覆盖
每个功能点必须预判异常场景（未登录、网络异常、数据为空、权限不足、操作失败）。**对异常场景的覆盖程度是区分资深 PM 和初级 PM 的关键**。

### 范围边界清晰
明确「不做什么」比明确「做什么」更有助于防止范围蔓延。Out of Scope 列表必须具体、有理由。

### 优先级驱动
每个功能点标注 P0/P1/P2，支撑团队在资源不足时做 MVP 裁剪。优先级判断标准：
- P0：没有它，这个需求做不了或毫无意义
- P1：没有它，核心体验不完整
- P2：有它更好，但不是必须

### 一句话总结前置
每个需求描述的开头用加粗的一句话总结。让读者扫一眼就知道这个功能点做什么，再决定要不要细读。

### 以读者为中心
PRD 是给别人看的，不是给自己看的。考虑不同角色的诉求：
- **设计师**需要看：背景、用户画像、原型描述
- **前端**需要看：交互说明、状态处理、验收标准、埋点需求
- **后端**需要看：功能清单、数据关系、异常处理、边界条件、接口逻辑
- **测试**需要看：功能清单、验收标准（Given/When/Then）、边界条件、异常场景
- **运营**需要看：背景、成功指标、功能价值

### 可测试性优先
每个功能点必须包含明确的验收标准（Given/When/Then），让测试可以直接转化为测试用例。避免「体验流畅」「操作便捷」等不可验证的表述。

---

## 产品类型适配

不同产品类型在模版中侧重点不同：

| 产品类型 | 重点调整 |
|----------|----------|
| **移动端 App** | 交互说明要覆盖手势、转场、原生组件；兼容性覆盖 iOS/Android 版本 |
| **小程序** | 注意平台能力限制（包大小、API 限制）；兼容性覆盖微信版本 |
| **Web 端** | 响应式布局说明；浏览器兼容性；SEO 影响（如涉及） |
| **SaaS** | 多租户权限模型；数据隔离；计费/定价影响 |
| **平台工具** | API/接口设计；开发者体验；文档和示例；向后兼容 |

---

## 输出格式

### 飞书文档模式

生成 PRD 后执行：
1. 调用 `lark-doc` 创建飞书文档，格式为 DocxXML，使用当前已认证的飞书用户身份；如需指定归属人，先询问用户
2. 将完整 PRD 内容写入飞书文档
3. 返回文档链接给用户
4. 询问是否需要创建关联 OKR，如需要则调用 `lark-okr`
5. 询问是否需要归档到知识库，如需要则调用 `lark-wiki`

### 本地 Markdown 模式

将 PRD 保存为 `.md` 文件到用户指定的目录（默认当前工作目录），文件名格式：`PRD-[产品名]-[版本号].md`。

---

## 参考

`references/prd-template.md` 包含完整的空白 PRD 模版（Markdown 格式），可直接复制使用。
